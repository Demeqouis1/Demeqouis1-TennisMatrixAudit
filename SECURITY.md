# Security

Secrets are environment-only and excluded from Git. Ordinary users and administrators have separate permissions. Document replacement, rule mapping, identity/surface overrides, calibration corrections, source blacklisting, grading policy changes, and final classification overrides are administrator actions and append immutable history.

Production deployments must use encrypted transport, least-privilege database credentials, audit logging, rate limits, backups, restore verification, and secret rotation.
